CREATE TABLE [dbo].[Coordinates] (
    [Id]       INT            IDENTITY (1, 1) NOT NULL,
    [GlobalId] NVARCHAR (MAX) NULL,
    [Float_0]  NVARCHAR (MAX) NULL,
    [Float_1]  NVARCHAR (MAX) NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

